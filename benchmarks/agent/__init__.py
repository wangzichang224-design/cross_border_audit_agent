"""Agent-side modules: parsing, filling, and the workpaper writer.

DO NOT IMPORT from benchmarks.ground_truth — the Agent must operate
without access to the answer key. CI/pre-commit checks enforce this.
"""
